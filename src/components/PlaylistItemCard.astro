---
import type { Playlist } from "../libreria/datos";
import { BotonPlayCard } from "./BotonPlayCard";

interface Props {
  playlist: Playlist;
}
const { playlist } = Astro.props;
const { id, cover, title, artists, color } = playlist;

const artistsString = artists.join(",");
---

<article
  class="group relative hover:bg-zinc-700/30 hover:shadow-xl mb-2 rounded-md transition-all
  duration-300"
>
  <div
    class="absolute right-4 bottom-24 traslate-y-4 transition-all duration-300 opacity-0 
    group-hover:traslate-y-0 group-hover:opacity-100 z-10"
  >
    <BotonPlayCard id={id} client:visible className="" size="medium"/>
  </div>
  <a
    href={`/playlist/${id}`}
    class="playlist-item transition-all duration-300 flex relative overflow-hidden flex-relative p-3 gap-6 pb-4
     w-40 flex-col font-bold"
    transition:name=`playlist ${id} box`
  >
    <picture class="aspect-square w-full h-auto flex-none">
      <img
        src={cover}
        alt={`Cover of ${title} by ${artistsString}`}
        class="object-cover w-full h-full rounded-md"
        transition:name=`playlsit ${id} image`
      />
    </picture>

    <div class="flex flex-auto flex-col px-2 truncate">
      <h4 class="text-white font-sm truncate">
        {title}
      </h4>
      <span class="text-gray-400 font-medium truncate">
        {artistsString}
      </span>
    </div>
  </a>
</article>
